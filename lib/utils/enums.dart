enum AuthStatus {
  done,
  unknownError,
  networkError,
  emailInUse,
  profileNotFound,
  weakPassword,
  wrongPassword,
}

enum SnackBarIcon {
  none,
  done,
  error,
  info,
}

enum TablesSortSetting {
  name,
  dateAdded,
}